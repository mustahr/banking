import Loader from "@/components/ui/Loader";

const TransactionHistory = () => {
  return (
    <div>
      <Loader />
      Transaction History
    </div>
  );
};

export default TransactionHistory;
